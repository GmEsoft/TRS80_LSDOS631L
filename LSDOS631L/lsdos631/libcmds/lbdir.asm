; LBDIR/ASM - DIR/CAT commands	TITLE	<DIR - LSDOS 6.3>;	ORG	2400H;;	Pete Cervasio's customization - define @PCERV to 0FFH;	to make DIR not clear the screen.;@PCERV	EQU	0		;Set to 0FFH to get rid of @CLS;;ENTRY	JP	DIR		; Go if DIR;CATBGN	PUSH	HL		; Here if CAT	LD	HL,0		; Set the DIR(A	LD	(APARM+1),HL	;   parameter to OFF	POP	HL		;   and do a DIR	JR	ENTRY		;   command;BLKHASH	EQU	4296H		; Hash code of blank password;*GET	SVCMAC			; Get SVC equates*GET	VALUES			; Other misc. equatesMONTBL$	EQU	04DCH		; LOWCORE month strings;*GET	LBDIRA*GET	LBDIRB*GET	LBDIRC;ENDMEM	EQU	$FREE$	EQU	3000H-ENDMEM;	IFGT	$,2FFFH	ERR	'LIB memory region overflow	ENDIF;	END	ENTRY