; ***************************************************************; * Filename: BACKUP/ASM					*; * Revision: 06.03.01						*; * Rev Date: 03 Dec 97						*; ***************************************************************; * DOS file and disk backup utility				*; *								*; ***************************************************************;*GET	BACKUP1;;	Save PC for later use;CORE$	DEFL	$<-8+1<+8	; Set to next page;	ORG	BACKUP		; Set for MIRROR execMIRBU	EQU	CORE$	LORG	MIRBU		; Load origin;	SUBTTL	'<Mirror Image Backup>';*GET	BACKUP2;MIRSIZ	EQU	$<-8+1<+8-BACKUP;;	Adjust PC and load address for CLASS backup;	ORG	BACKUP;CLSBU	EQU	CORE$+MIRSIZ	LORG	CLSBU;	SUBTTL	'<Backup By Class>';*GET	BACKUP3;LAST	EQU	$;	END	BACKUP